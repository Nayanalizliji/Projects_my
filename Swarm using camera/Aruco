import cv2
aruco_dict = cv2.aruco.getPredefinedDictionary(cv2.aruco.DICT_4X4_50)
for id in range(5):  # Generate 5 markers
    marker = cv2.aruco.generateImageMarker(aruco_dict, id, 200)
    cv2.imwrite(f"aruco_{id}.png", marker)
